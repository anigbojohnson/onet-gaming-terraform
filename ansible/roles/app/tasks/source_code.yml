- name: Ensure node user exists
  ansible.builtin.user:
    name: node
    create_home: yes

- name: Copy Node.js application to server
  ansible.builtin.synchronize:
    src: "{{ playbook_dir }}/../server/"
    dest: /var/www/server/
    recursive: yes
    rsync_opts:
      - "--chown=node:node"
